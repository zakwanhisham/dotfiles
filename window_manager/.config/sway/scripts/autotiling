#!/usr/bin/env bash

# Check if autotiling process exists and kill it
if pgrep -x "autotiling" >/dev/null; then
    pkill -x "autotiling"
    # Wait a moment for the process to terminate
    sleep 1
fi

# Start autotiling with specified parameters
/usr/bin/autotiling -sw 0.65 -sr 1.61 &
