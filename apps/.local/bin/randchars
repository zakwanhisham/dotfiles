#!/usr/bin/env python3

import string
import random

def main() -> None:
    length: int = 8
    chars: str = string.ascii_letters + string.digits
    random_str: str = "".join(random.choices(chars, k=length))

    return print(random_str)

if __name__ == "__main__":
    main()
